services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: steward
      POSTGRES_PASSWORD: steward_dev_password
      POSTGRES_DB: steward
      ports:
        - "5432:5432"
      volumes:
        - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: